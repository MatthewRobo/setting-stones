if (instance_exists(summoner)) {
	trail_points = summoner.trail_points;
	color_slow = summoner.color_slow;
	color_fast = summoner.color_fast;
	shield_active = summoner.shield_active;
	damage_mult = summoner.damage_mult;
	heat = summoner.heat;
}

